version: 2

sources:
  - name: "my"
    meta:
      external_location: "/Users/henkgriffioen/gdd/dbt-excel/dbt_project/{name}.xlsx"
    tables:
      - name: excel_source

models:
  - name: my_first_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
